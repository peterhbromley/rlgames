[project]
name = "rlgames"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy",
    "open_spiel",
    "jupyter",
    "torch>=2.10.0",
    "matplotlib>=3.10.8",
]
